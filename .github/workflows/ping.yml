name: Keep Server Running

on:
  schedule:
    - cron: "*/10 * * * *"   # Every 10 minutes
  workflow_dispatch:         # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping deployed backend
        run: |
          curl -I https://retail-sales-system.onrender.com || true
